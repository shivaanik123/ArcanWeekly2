# S3-Only Storage Configuration
# S3 bucket name where data files will be stored
S3_BUCKET_NAME=your-bucket-name

# S3 prefix/folder for data files (optional, defaults to 'data/')
S3_DATA_PREFIX=data/

# AWS Credentials
# You can also use AWS CLI profiles or IAM roles instead of explicit keys
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1

# Production Example:
# S3_BUCKET_NAME=arcan-dashboard-data-dev-123456789
# S3_DATA_PREFIX=data/
# AWS_REGION=us-east-1